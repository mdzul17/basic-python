Tempatkan gambar leaderboard yang ingin diproses di folder ini.

Format yang didukung: `.jpg`, `.jpeg`, `.png`, `.webp`, `.bmp`, `.tif`, dan `.tiff`.

Contoh:

```bash
cp /path/ke/leaderboard.jpg datasets/leaderboard.jpg
python3 ocr_leaderboard.py
```
